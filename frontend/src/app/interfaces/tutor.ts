export interface Tutor {
    firstName: String,
    lastName: String,
    email: String,
    bio: String,
    suId: Number,
    available: boolean
    workingHours: Number,
    isSelected: boolean
   
}